UNE STACK		|	UNE LISTE DE TOKENS


1) on regarde toujours, pour le state 0, le 1er element de la stack (au debut donc, vide) , donc on essaie de shift un truc avec la tete de notre liste de tokens.

Si le 1er element de la stack est utilisable : 

	- si on doit shift : on va a la state correspondante, on deplace le curseur a l'element suivant dans la stack. Si il n'y a pas d'element suivant dans la stack, on ajoute notre tete de liste de tokens.
	- on va a la state correspondante, on deplace le curseur a l'element suivant dans la stack
	
Sinon on ajoute notre tete de liste de tokens
